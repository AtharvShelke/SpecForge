
squeeze the header ui of products page
also squeeze the products detail page
remove the review system only from ui
fix the fiulter sidebar for hte processors, when none is selected all of the filters should appear, remove the search in description and search inb sub Categories for processor, also remove the brand filter because it does not work, but removes everything from the sidebar the current manufacturer is working fine for the sidebar
categories bar and sub categories bar should have better navigation elements for the categories on far right or left
admin login page
show gst to the customer as well, i made an order and it showed only the base amount add gst to it for better transparency to the customer
## error
PATCH /api/orders/[id] error: Error [PrismaClientKnownRequestError]: Transaction API error: Unable to start a transaction in the given time.
    at async PATCH (app\api\orders\[id]\route.ts:50:23)
  48 |         const data = updateOrderSchema.parse(body);
  49 |
> 50 |         const order = await prisma.$transaction(async (tx) => {
     |                       ^
  51 |             const existing = await tx.order.findUnique({
  52 |                 where: { id },
  53 |                 include: { items: true }, {
  code: 'P2028',
  meta: {},
  clientVersion: '7.4.1'
}
 PATCH /api/orders/ORD-VOSWJ4B 500 in 5.7s (compile: 3.1s, render: 2.6s)
worked fine in another try
inventory page values are not shown
inventory adjust reason should be optional
POST /api/inventory/CPU-AMD-7800X3D/movements 404 in 2.8s (compile: 1865ms, render: 915ms)  
uploadthing setup
landing page UI
meta data